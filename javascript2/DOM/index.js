// This is about interacting with the DOM, we use document to specify the dom
let getDom = document.body;
let getHead = document.head;
console.log(getDom);
console.log(getHead);
console.log(document.title);

// setTimeout(() => {
//   document.body.style.backgroundColor = 'lightgreen'; // this change the background color after 300ms/3sec
// }, 3000);
